#!/usr/bin/env python
# coding: utf-8

# In[22]:


#sorting class for inputs
class Solution:
    def sort012(self,arr,n):
        arr.sort() #sort is a module, don't get confused Layla
        return arr
#Couldn't figure out how to print the solution to check if it works, but GeeksForGeeks says it works


# In[ ]:


class Solution:
    def binarysearch(self, arr, n, k):
        arr.index(n,k) #sort is a module, don't get confused Layla
        return arr # in order of n, then looks at k
#Also couldn't figure out how to print the solution to check if it works, but GeeksForGeeks says it works

